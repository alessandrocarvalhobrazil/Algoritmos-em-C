#include <stdio.h>
#include <math.h>

int main(void) {

  float valor, raiz;

  printf("Informe um número:");
  scanf("%f", &valor);
  
    if (valor/2==0);
    {
      raiz= sqrt(valor);
      printf("A raiz quadrada de %f é %.2f", valor, raiz);

    }
  return 0;
}
