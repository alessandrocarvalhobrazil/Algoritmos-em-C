#include <stdio.h>
#include <math.h>

int main(void)
{

int valor, ant, suc;

  printf("Digite um número:");
  scanf("%d",&valor);

  ant= valor-1;
  suc= valor+1;

  printf("\nO sucesso de %d é %d",valor,suc);
  printf("\nO antecessor de %d é %d", valor, ant);
    
 return 0;
}
