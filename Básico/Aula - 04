#include <stdio.h>
#include <math.h>

int main(void) {
  int valor;

    printf("Digite um número inteiro:\n");
    scanf("%d", &valor);

    printf("\n A potência de %d é %.2f",valor, pow(valor,2));
    printf("\n A raiz quadrada de %d é %.2f", valor, sqrt(valor));



  return 0;
}
